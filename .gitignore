# Gradle
.gradle/
build/
.kotlin/

# Local configuration
local.properties

# IDE
.idea/
*.iml
captures/

# macOS
.DS_Store

# Impeccable skill: web-only tooling (detector, live browser) is inert for a
# native Android app — keep the skill + references, skip 2.9M of dead weight.
# Restore anytime with: npx impeccable install
.claude/skills/impeccable/scripts/

# Third-party (Mobbin) reference captures — 600 MB, never commit; curated webp picks live in docs/design/refs/strava/
docs/design/refs/Strava*/
